name: Vikunja task manager contract

on:
  pull_request:
    paths:
      - 'ods/extensions/library/services/vikunja/**'
      - 'ods/tests/test_vikunja_extension.py'
      - '.github/workflows/vikunja-extension.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  vikunja-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install pytest PyYAML
      - name: Verify Vikunja extension contract
        run: python -m pytest -q ods/tests/test_vikunja_extension.py
