name: Wallabag article archiver contract

on:
  pull_request:
    paths:
      - 'ods/extensions/library/services/wallabag/**'
      - 'ods/tests/test_wallabag_extension.py'
      - '.github/workflows/wallabag-extension.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  wallabag-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install pytest PyYAML
      - name: Verify Wallabag extension contract
        run: python -m pytest -q ods/tests/test_wallabag_extension.py
