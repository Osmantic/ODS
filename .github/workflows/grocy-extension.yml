name: Grocy household management contract

on:
  pull_request:
    paths:
      - 'ods/extensions/library/services/grocy/**'
      - 'ods/tests/test_grocy_extension.py'
      - '.github/workflows/grocy-extension.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  grocy-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install pytest PyYAML
      - name: Verify Grocy extension contract
        run: python -m pytest -q ods/tests/test_grocy_extension.py
