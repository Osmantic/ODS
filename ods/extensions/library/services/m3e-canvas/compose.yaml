services:
  m3e-canvas:
    image: ods/m3e-canvas:dea7415-local-v1
    container_name: ods-m3e-canvas
    restart: unless-stopped
    security_opt:
    - no-new-privileges:true
    ports:
    - 127.0.0.1:${M3E_CANVAS_PORT:-11074}:8080
    healthcheck:
      test:
      - CMD
      - wget
      - --spider
      - -q
      - -T
      - '5'
      - http://127.0.0.1:8080/
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    networks:
    - ods-network
    user: 101:101
    build:
      context: .
      dockerfile: Dockerfile
    command:
    - -g
    - daemon off;
    entrypoint:
    - nginx
    read_only: true
    tmpfs:
    - /tmp:rw,noexec,nosuid,size=32m
networks:
  ods-network:
    external: true
    name: ods-network
