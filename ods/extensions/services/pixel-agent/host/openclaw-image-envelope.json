{
  "sourceSha256": "9597e4765bdcafc5f51ba0640c94790dfd5fc5445879408827524ff4b02a78f0",
  "patchedSha256": "a6e4f7d12b0ceeeb4c8369a3d1a890a1e1ecd6cf743d45649d3e48446d1c6b30",
  "replacements": [
    [
      "async function runCodeMode(params) {",
      "/** Preserve native images instead of JSON-encoding their bytes as prompt text. */\nfunction toolCallResultEnvelope(payload) {\n\tconst result = isRecord(payload?.result) ? payload.result : void 0;\n\tconst content = result?.content;\n\tconst validImage = (block) => isRecord(block) && block.type === \"image\"\n\t\t&& typeof block.data === \"string\" && block.data.length > 0\n\t\t&& typeof block.mimeType === \"string\" && /^image\\/[a-z0-9.+-]+$/i.test(block.mimeType);\n\tconst validText = (block) => isRecord(block) && block.type === \"text\" && typeof block.text === \"string\";\n\t// Leave unfamiliar result shapes to the existing serializer. Do not silently\n\t// drop unsupported blocks or claim a malformed image is a native attachment.\n\tif (!Array.isArray(content)\n\t\t|| !content.every((block) => validText(block) || validImage(block))) return jsonResult(payload);\n\tif (!content.some(validImage)) {\n\t\t// jsonResult tools may include their entire details object twice: once as\n\t\t// JSON text and once as structured metadata. Project only a byte-identical\n\t\t// JSON rendering out of the model-visible envelope; retain the original\n\t\t// payload out of band for framework consumers and provenance.\n\t\tif (content.length === 1 && validText(content[0]) && isRecord(result.details)\n\t\t\t&& content[0].text === JSON.stringify(result.details, null, 2)) {\n\t\t\tconst projected = { ...result };\n\t\t\tdelete projected.details;\n\t\t\treturn { ...jsonResult({ ...payload, result: projected }), details: payload };\n\t\t}\n\t\t// Exec may repeat the same bytes in content and details.aggregated.\n\t\t// Remove only an exact duplicate from model-visible JSON; distinct output,\n\t\t// status, opaque process handles and the original provenance remain intact.\n\t\tif (content.length === 1 && validText(content[0]) && isRecord(result.details)\n\t\t\t&& result.details.aggregated === content[0].text) {\n\t\t\tconst details = { ...result.details };\n\t\t\tdelete details.aggregated;\n\t\t\treturn { ...jsonResult({ ...payload, result: { ...result, details } }), details: payload };\n\t\t}\n\t\treturn jsonResult(payload);\n\t}\n\tconst tool = isRecord(payload.tool) ? payload.tool : {};\n\tconst identity = Object.fromEntries([\"id\", \"source\", \"sourceName\", \"name\"]\n\t\t.filter((key) => typeof tool[key] === \"string\").map((key) => [key, tool[key]]));\n\tconst envelope = {\n\t\t...result,\n\t\tcontent: [\n\t\t\t...(Object.keys(identity).length ? [{ type: \"text\", text: JSON.stringify({ tool: identity }) }] : []),\n\t\t\t...content\n\t\t],\n\t\t// Framework-owned provenance is retained for ODS receipts and adapters.\n\t\tdetails: payload\n\t};\n\tif (payload.isError === true || result.isError === true) envelope.isError = true;\n\treturn envelope;\n}\nasync function runCodeMode(params) {"
    ],
    [
      "return jsonResult(await runtime.call(call.id, call.input, {",
      "return toolCallResultEnvelope(await runtime.call(call.id, call.input, {"
    ],
    [
      "function applyToolSearchCatalog(params) {\n\tconst config = resolveToolSearchConfig(params.config);\n\treturn applyToolCatalogCompaction({\n\t\t...params,\n\t\tenabled: config.enabled,\n\t\tisVisibleControlTool: (tool) => TOOL_SEARCH_CONTROL_TOOL_NAMES.has(tool.name) && shouldExposeControlTool(tool.name, config.mode)\n\t});\n}",
      "function applyToolSearchCatalog(params) {\n\tconst config = resolveToolSearchConfig(params.config);\n\t// ODS exposes a stable working surface, never a prompt-keyword tool plan.\n\t// Policy has already filtered params.tools; every other permitted tool stays\n\t// in the catalog. Ambiguous names remain deferred, with qualified IDs.\n\tconst odsNativeNames = params.agentId === \"pixel\" && config.mode === \"tools\"\n\t\t? new Set([\"read\", \"write\", \"edit\", \"apply_patch\", \"exec\", \"process\",\n\t\t\t\"web_fetch\", \"web_search\", \"pixel_ods_skill\", \"pixel_ods_ask_user\",\n\t\t\t\"pixel_ods_extensions\", \"pixel_ods_extension_request_status\",\n\t\t\t\"pixel_ods_extension_request_prepare\", \"pixel_ods_extension_request_advance\", \"pixel_ods_extension_request_retry\",\n\t\t\t\"pixel_ods_source_proposal\", \"pixel_ods_python_library_proposal\"])\n\t\t: new Set();\n\tconst uniqueNames = collectUniqueCatalogToolNames(params.tools);\n\treturn applyToolCatalogCompaction({\n\t\t...params,\n\t\tenabled: config.enabled,\n\t\t...(odsNativeNames.size ? {\n\t\t\tisVisibleCatalogTool: (tool) => odsNativeNames.has(tool.name) && uniqueNames.has(tool.name)\n\t\t} : {}),\n\t\tisVisibleControlTool: (tool) => TOOL_SEARCH_CONTROL_TOOL_NAMES.has(tool.name) && shouldExposeControlTool(tool.name, config.mode)\n\t});\n}"
    ]
  ],
  "previousReplacements": {
    "4958ed801841f3659ee722dd1d3e5006d91440c9e214afac2770e8258448efdb": [
      [
        "async function runCodeMode(params) {",
        "/** Preserve native images instead of JSON-encoding their bytes as prompt text. */\nfunction toolCallResultEnvelope(payload) {\n\tconst result = isRecord(payload?.result) ? payload.result : void 0;\n\tconst content = result?.content;\n\tconst validImage = (block) => isRecord(block) && block.type === \"image\"\n\t\t&& typeof block.data === \"string\" && block.data.length > 0\n\t\t&& typeof block.mimeType === \"string\" && /^image\\/[a-z0-9.+-]+$/i.test(block.mimeType);\n\tconst validText = (block) => isRecord(block) && block.type === \"text\" && typeof block.text === \"string\";\n\t// Leave unfamiliar result shapes to the existing serializer. Do not silently\n\t// drop unsupported blocks or claim a malformed image is a native attachment.\n\tif (!Array.isArray(content) || !content.some(validImage)\n\t\t|| !content.every((block) => validText(block) || validImage(block))) return jsonResult(payload);\n\tconst tool = isRecord(payload.tool) ? payload.tool : {};\n\tconst identity = Object.fromEntries([\"id\", \"source\", \"sourceName\", \"name\"]\n\t\t.filter((key) => typeof tool[key] === \"string\").map((key) => [key, tool[key]]));\n\tconst envelope = {\n\t\t...result,\n\t\tcontent: [\n\t\t\t...(Object.keys(identity).length ? [{ type: \"text\", text: JSON.stringify({ tool: identity }) }] : []),\n\t\t\t...content\n\t\t],\n\t\t// Framework-owned provenance is retained for ODS receipts and adapters.\n\t\tdetails: payload\n\t};\n\tif (payload.isError === true || result.isError === true) envelope.isError = true;\n\treturn envelope;\n}\nasync function runCodeMode(params) {"
      ],
      [
        "return jsonResult(await runtime.call(call.id, call.input, {",
        "return toolCallResultEnvelope(await runtime.call(call.id, call.input, {"
      ]
    ],
    "b17069bba2f0728fbf37eca610d70695f2cc89be83594d969b7667ab966ee175": [
      [
        "async function runCodeMode(params) {",
        "/** Preserve native images instead of JSON-encoding their bytes as prompt text. */\nfunction toolCallResultEnvelope(payload) {\n\tconst result = isRecord(payload?.result) ? payload.result : void 0;\n\tconst content = result?.content;\n\tconst validImage = (block) => isRecord(block) && block.type === \"image\"\n\t\t&& typeof block.data === \"string\" && block.data.length > 0\n\t\t&& typeof block.mimeType === \"string\" && /^image\\/[a-z0-9.+-]+$/i.test(block.mimeType);\n\tconst validText = (block) => isRecord(block) && block.type === \"text\" && typeof block.text === \"string\";\n\t// Leave unfamiliar result shapes to the existing serializer. Do not silently\n\t// drop unsupported blocks or claim a malformed image is a native attachment.\n\tif (!Array.isArray(content)\n\t\t|| !content.every((block) => validText(block) || validImage(block))) return jsonResult(payload);\n\tif (!content.some(validImage)) {\n\t\t// Exec may repeat the same bytes in content and details.aggregated.\n\t\t// Remove only an exact duplicate from model-visible JSON; distinct output,\n\t\t// status, opaque process handles and the original provenance remain intact.\n\t\tif (content.length === 1 && validText(content[0]) && isRecord(result.details)\n\t\t\t&& result.details.aggregated === content[0].text) {\n\t\t\tconst details = { ...result.details };\n\t\t\tdelete details.aggregated;\n\t\t\treturn { ...jsonResult({ ...payload, result: { ...result, details } }), details: payload };\n\t\t}\n\t\treturn jsonResult(payload);\n\t}\n\tconst tool = isRecord(payload.tool) ? payload.tool : {};\n\tconst identity = Object.fromEntries([\"id\", \"source\", \"sourceName\", \"name\"]\n\t\t.filter((key) => typeof tool[key] === \"string\").map((key) => [key, tool[key]]));\n\tconst envelope = {\n\t\t...result,\n\t\tcontent: [\n\t\t\t...(Object.keys(identity).length ? [{ type: \"text\", text: JSON.stringify({ tool: identity }) }] : []),\n\t\t\t...content\n\t\t],\n\t\t// Framework-owned provenance is retained for ODS receipts and adapters.\n\t\tdetails: payload\n\t};\n\tif (payload.isError === true || result.isError === true) envelope.isError = true;\n\treturn envelope;\n}\nasync function runCodeMode(params) {"
      ],
      [
        "return jsonResult(await runtime.call(call.id, call.input, {",
        "return toolCallResultEnvelope(await runtime.call(call.id, call.input, {"
      ]
    ],
    "64da9713ed055e94566d116e7e366652a5cc0f3868a874fb031e273d410edd42": [
      [
        "async function runCodeMode(params) {",
        "/** Preserve native images instead of JSON-encoding their bytes as prompt text. */\nfunction toolCallResultEnvelope(payload) {\n\tconst result = isRecord(payload?.result) ? payload.result : void 0;\n\tconst content = result?.content;\n\tconst validImage = (block) => isRecord(block) && block.type === \"image\"\n\t\t&& typeof block.data === \"string\" && block.data.length > 0\n\t\t&& typeof block.mimeType === \"string\" && /^image\\/[a-z0-9.+-]+$/i.test(block.mimeType);\n\tconst validText = (block) => isRecord(block) && block.type === \"text\" && typeof block.text === \"string\";\n\t// Leave unfamiliar result shapes to the existing serializer. Do not silently\n\t// drop unsupported blocks or claim a malformed image is a native attachment.\n\tif (!Array.isArray(content)\n\t\t|| !content.every((block) => validText(block) || validImage(block))) return jsonResult(payload);\n\tif (!content.some(validImage)) {\n\t\t// jsonResult tools may include their entire details object twice: once as\n\t\t// JSON text and once as structured metadata. Project only a byte-identical\n\t\t// JSON rendering out of the model-visible envelope; retain the original\n\t\t// payload out of band for framework consumers and provenance.\n\t\tif (content.length === 1 && validText(content[0]) && isRecord(result.details)\n\t\t\t&& content[0].text === JSON.stringify(result.details, null, 2)) {\n\t\t\tconst projected = { ...result };\n\t\t\tdelete projected.details;\n\t\t\treturn { ...jsonResult({ ...payload, result: projected }), details: payload };\n\t\t}\n\t\t// Exec may repeat the same bytes in content and details.aggregated.\n\t\t// Remove only an exact duplicate from model-visible JSON; distinct output,\n\t\t// status, opaque process handles and the original provenance remain intact.\n\t\tif (content.length === 1 && validText(content[0]) && isRecord(result.details)\n\t\t\t&& result.details.aggregated === content[0].text) {\n\t\t\tconst details = { ...result.details };\n\t\t\tdelete details.aggregated;\n\t\t\treturn { ...jsonResult({ ...payload, result: { ...result, details } }), details: payload };\n\t\t}\n\t\treturn jsonResult(payload);\n\t}\n\tconst tool = isRecord(payload.tool) ? payload.tool : {};\n\tconst identity = Object.fromEntries([\"id\", \"source\", \"sourceName\", \"name\"]\n\t\t.filter((key) => typeof tool[key] === \"string\").map((key) => [key, tool[key]]));\n\tconst envelope = {\n\t\t...result,\n\t\tcontent: [\n\t\t\t...(Object.keys(identity).length ? [{ type: \"text\", text: JSON.stringify({ tool: identity }) }] : []),\n\t\t\t...content\n\t\t],\n\t\t// Framework-owned provenance is retained for ODS receipts and adapters.\n\t\tdetails: payload\n\t};\n\tif (payload.isError === true || result.isError === true) envelope.isError = true;\n\treturn envelope;\n}\nasync function runCodeMode(params) {"
      ],
      [
        "return jsonResult(await runtime.call(call.id, call.input, {",
        "return toolCallResultEnvelope(await runtime.call(call.id, call.input, {"
      ]
    ],
    "4591dff37a4956a277b83a2083eb5e52aff65fb313ebbc1de88cb8d39016e940": [
      [
        "async function runCodeMode(params) {",
        "/** Preserve native images instead of JSON-encoding their bytes as prompt text. */\nfunction toolCallResultEnvelope(payload) {\n\tconst result = isRecord(payload?.result) ? payload.result : void 0;\n\tconst content = result?.content;\n\tconst validImage = (block) => isRecord(block) && block.type === \"image\"\n\t\t&& typeof block.data === \"string\" && block.data.length > 0\n\t\t&& typeof block.mimeType === \"string\" && /^image\\/[a-z0-9.+-]+$/i.test(block.mimeType);\n\tconst validText = (block) => isRecord(block) && block.type === \"text\" && typeof block.text === \"string\";\n\t// Leave unfamiliar result shapes to the existing serializer. Do not silently\n\t// drop unsupported blocks or claim a malformed image is a native attachment.\n\tif (!Array.isArray(content)\n\t\t|| !content.every((block) => validText(block) || validImage(block))) return jsonResult(payload);\n\tif (!content.some(validImage)) {\n\t\t// jsonResult tools may include their entire details object twice: once as\n\t\t// JSON text and once as structured metadata. Project only a byte-identical\n\t\t// JSON rendering out of the model-visible envelope; retain the original\n\t\t// payload out of band for framework consumers and provenance.\n\t\tif (content.length === 1 && validText(content[0]) && isRecord(result.details)\n\t\t\t&& content[0].text === JSON.stringify(result.details, null, 2)) {\n\t\t\tconst projected = { ...result };\n\t\t\tdelete projected.details;\n\t\t\treturn { ...jsonResult({ ...payload, result: projected }), details: payload };\n\t\t}\n\t\t// Exec may repeat the same bytes in content and details.aggregated.\n\t\t// Remove only an exact duplicate from model-visible JSON; distinct output,\n\t\t// status, opaque process handles and the original provenance remain intact.\n\t\tif (content.length === 1 && validText(content[0]) && isRecord(result.details)\n\t\t\t&& result.details.aggregated === content[0].text) {\n\t\t\tconst details = { ...result.details };\n\t\t\tdelete details.aggregated;\n\t\t\treturn { ...jsonResult({ ...payload, result: { ...result, details } }), details: payload };\n\t\t}\n\t\treturn jsonResult(payload);\n\t}\n\tconst tool = isRecord(payload.tool) ? payload.tool : {};\n\tconst identity = Object.fromEntries([\"id\", \"source\", \"sourceName\", \"name\"]\n\t\t.filter((key) => typeof tool[key] === \"string\").map((key) => [key, tool[key]]));\n\tconst envelope = {\n\t\t...result,\n\t\tcontent: [\n\t\t\t...(Object.keys(identity).length ? [{ type: \"text\", text: JSON.stringify({ tool: identity }) }] : []),\n\t\t\t...content\n\t\t],\n\t\t// Framework-owned provenance is retained for ODS receipts and adapters.\n\t\tdetails: payload\n\t};\n\tif (payload.isError === true || result.isError === true) envelope.isError = true;\n\treturn envelope;\n}\nasync function runCodeMode(params) {"
      ],
      [
        "return jsonResult(await runtime.call(call.id, call.input, {",
        "return toolCallResultEnvelope(await runtime.call(call.id, call.input, {"
      ],
      [
        "function applyToolSearchCatalog(params) {\n\tconst config = resolveToolSearchConfig(params.config);\n\treturn applyToolCatalogCompaction({\n\t\t...params,\n\t\tenabled: config.enabled,\n\t\tisVisibleControlTool: (tool) => TOOL_SEARCH_CONTROL_TOOL_NAMES.has(tool.name) && shouldExposeControlTool(tool.name, config.mode)\n\t});\n}",
        "function applyToolSearchCatalog(params) {\n\tconst config = resolveToolSearchConfig(params.config);\n\t// ODS exposes a stable working surface, never a prompt-keyword tool plan.\n\t// Policy has already filtered params.tools; every other permitted tool stays\n\t// in the catalog. Ambiguous names remain deferred, with qualified IDs.\n\tconst odsNativeNames = params.agentId === \"pixel\" && config.mode === \"tools\"\n\t\t? new Set([\"read\", \"write\", \"edit\", \"apply_patch\", \"exec\", \"process\",\n\t\t\t\"web_fetch\", \"web_search\", \"pixel_ods_skill\", \"pixel_ods_ask_user\",\n\t\t\t\"pixel_ods_extensions\", \"pixel_ods_extension_request_status\"])\n\t\t: new Set();\n\tconst uniqueNames = collectUniqueCatalogToolNames(params.tools);\n\treturn applyToolCatalogCompaction({\n\t\t...params,\n\t\tenabled: config.enabled,\n\t\t...(odsNativeNames.size ? {\n\t\t\tisVisibleCatalogTool: (tool) => odsNativeNames.has(tool.name) && uniqueNames.has(tool.name)\n\t\t} : {}),\n\t\tisVisibleControlTool: (tool) => TOOL_SEARCH_CONTROL_TOOL_NAMES.has(tool.name) && shouldExposeControlTool(tool.name, config.mode)\n\t});\n}"
      ]
    ],
    "71d320e6d155f21d08c9448a9fcf3fc8bf390e874391bde3dd5f7406e1ab82d2": [
      [
        "async function runCodeMode(params) {",
        "/** Preserve native images instead of JSON-encoding their bytes as prompt text. */\nfunction toolCallResultEnvelope(payload) {\n\tconst result = isRecord(payload?.result) ? payload.result : void 0;\n\tconst content = result?.content;\n\tconst validImage = (block) => isRecord(block) && block.type === \"image\"\n\t\t&& typeof block.data === \"string\" && block.data.length > 0\n\t\t&& typeof block.mimeType === \"string\" && /^image\\/[a-z0-9.+-]+$/i.test(block.mimeType);\n\tconst validText = (block) => isRecord(block) && block.type === \"text\" && typeof block.text === \"string\";\n\t// Leave unfamiliar result shapes to the existing serializer. Do not silently\n\t// drop unsupported blocks or claim a malformed image is a native attachment.\n\tif (!Array.isArray(content)\n\t\t|| !content.every((block) => validText(block) || validImage(block))) return jsonResult(payload);\n\tif (!content.some(validImage)) {\n\t\t// jsonResult tools may include their entire details object twice: once as\n\t\t// JSON text and once as structured metadata. Project only a byte-identical\n\t\t// JSON rendering out of the model-visible envelope; retain the original\n\t\t// payload out of band for framework consumers and provenance.\n\t\tif (content.length === 1 && validText(content[0]) && isRecord(result.details)\n\t\t\t&& content[0].text === JSON.stringify(result.details, null, 2)) {\n\t\t\tconst projected = { ...result };\n\t\t\tdelete projected.details;\n\t\t\treturn { ...jsonResult({ ...payload, result: projected }), details: payload };\n\t\t}\n\t\t// Exec may repeat the same bytes in content and details.aggregated.\n\t\t// Remove only an exact duplicate from model-visible JSON; distinct output,\n\t\t// status, opaque process handles and the original provenance remain intact.\n\t\tif (content.length === 1 && validText(content[0]) && isRecord(result.details)\n\t\t\t&& result.details.aggregated === content[0].text) {\n\t\t\tconst details = { ...result.details };\n\t\t\tdelete details.aggregated;\n\t\t\treturn { ...jsonResult({ ...payload, result: { ...result, details } }), details: payload };\n\t\t}\n\t\treturn jsonResult(payload);\n\t}\n\tconst tool = isRecord(payload.tool) ? payload.tool : {};\n\tconst identity = Object.fromEntries([\"id\", \"source\", \"sourceName\", \"name\"]\n\t\t.filter((key) => typeof tool[key] === \"string\").map((key) => [key, tool[key]]));\n\tconst envelope = {\n\t\t...result,\n\t\tcontent: [\n\t\t\t...(Object.keys(identity).length ? [{ type: \"text\", text: JSON.stringify({ tool: identity }) }] : []),\n\t\t\t...content\n\t\t],\n\t\t// Framework-owned provenance is retained for ODS receipts and adapters.\n\t\tdetails: payload\n\t};\n\tif (payload.isError === true || result.isError === true) envelope.isError = true;\n\treturn envelope;\n}\nasync function runCodeMode(params) {"
      ],
      [
        "return jsonResult(await runtime.call(call.id, call.input, {",
        "return toolCallResultEnvelope(await runtime.call(call.id, call.input, {"
      ],
      [
        "function applyToolSearchCatalog(params) {\n\tconst config = resolveToolSearchConfig(params.config);\n\treturn applyToolCatalogCompaction({\n\t\t...params,\n\t\tenabled: config.enabled,\n\t\tisVisibleControlTool: (tool) => TOOL_SEARCH_CONTROL_TOOL_NAMES.has(tool.name) && shouldExposeControlTool(tool.name, config.mode)\n\t});\n}",
        "function applyToolSearchCatalog(params) {\n\tconst config = resolveToolSearchConfig(params.config);\n\t// ODS exposes a stable working surface, never a prompt-keyword tool plan.\n\t// Policy has already filtered params.tools; every other permitted tool stays\n\t// in the catalog. Ambiguous names remain deferred, with qualified IDs.\n\tconst odsNativeNames = params.agentId === \"pixel\" && config.mode === \"tools\"\n\t\t? new Set([\"read\", \"write\", \"edit\", \"apply_patch\", \"exec\", \"process\",\n\t\t\t\"web_fetch\", \"web_search\", \"pixel_ods_skill\", \"pixel_ods_ask_user\",\n\t\t\t\"pixel_ods_extensions\", \"pixel_ods_extension_request_status\",\n\t\t\t\"pixel_ods_extension_request_prepare\", \"pixel_ods_extension_request_advance\"])\n\t\t: new Set();\n\tconst uniqueNames = collectUniqueCatalogToolNames(params.tools);\n\treturn applyToolCatalogCompaction({\n\t\t...params,\n\t\tenabled: config.enabled,\n\t\t...(odsNativeNames.size ? {\n\t\t\tisVisibleCatalogTool: (tool) => odsNativeNames.has(tool.name) && uniqueNames.has(tool.name)\n\t\t} : {}),\n\t\tisVisibleControlTool: (tool) => TOOL_SEARCH_CONTROL_TOOL_NAMES.has(tool.name) && shouldExposeControlTool(tool.name, config.mode)\n\t});\n}"
      ]
    ],
    "6cdb11c045487760202f7a718223cd6be96b07986d1b8fbf12633a64f1f54d67": [
      [
        "async function runCodeMode(params) {",
        "/** Preserve native images instead of JSON-encoding their bytes as prompt text. */\nfunction toolCallResultEnvelope(payload) {\n\tconst result = isRecord(payload?.result) ? payload.result : void 0;\n\tconst content = result?.content;\n\tconst validImage = (block) => isRecord(block) && block.type === \"image\"\n\t\t&& typeof block.data === \"string\" && block.data.length > 0\n\t\t&& typeof block.mimeType === \"string\" && /^image\\/[a-z0-9.+-]+$/i.test(block.mimeType);\n\tconst validText = (block) => isRecord(block) && block.type === \"text\" && typeof block.text === \"string\";\n\t// Leave unfamiliar result shapes to the existing serializer. Do not silently\n\t// drop unsupported blocks or claim a malformed image is a native attachment.\n\tif (!Array.isArray(content)\n\t\t|| !content.every((block) => validText(block) || validImage(block))) return jsonResult(payload);\n\tif (!content.some(validImage)) {\n\t\t// jsonResult tools may include their entire details object twice: once as\n\t\t// JSON text and once as structured metadata. Project only a byte-identical\n\t\t// JSON rendering out of the model-visible envelope; retain the original\n\t\t// payload out of band for framework consumers and provenance.\n\t\tif (content.length === 1 && validText(content[0]) && isRecord(result.details)\n\t\t\t&& content[0].text === JSON.stringify(result.details, null, 2)) {\n\t\t\tconst projected = { ...result };\n\t\t\tdelete projected.details;\n\t\t\treturn { ...jsonResult({ ...payload, result: projected }), details: payload };\n\t\t}\n\t\t// Exec may repeat the same bytes in content and details.aggregated.\n\t\t// Remove only an exact duplicate from model-visible JSON; distinct output,\n\t\t// status, opaque process handles and the original provenance remain intact.\n\t\tif (content.length === 1 && validText(content[0]) && isRecord(result.details)\n\t\t\t&& result.details.aggregated === content[0].text) {\n\t\t\tconst details = { ...result.details };\n\t\t\tdelete details.aggregated;\n\t\t\treturn { ...jsonResult({ ...payload, result: { ...result, details } }), details: payload };\n\t\t}\n\t\treturn jsonResult(payload);\n\t}\n\tconst tool = isRecord(payload.tool) ? payload.tool : {};\n\tconst identity = Object.fromEntries([\"id\", \"source\", \"sourceName\", \"name\"]\n\t\t.filter((key) => typeof tool[key] === \"string\").map((key) => [key, tool[key]]));\n\tconst envelope = {\n\t\t...result,\n\t\tcontent: [\n\t\t\t...(Object.keys(identity).length ? [{ type: \"text\", text: JSON.stringify({ tool: identity }) }] : []),\n\t\t\t...content\n\t\t],\n\t\t// Framework-owned provenance is retained for ODS receipts and adapters.\n\t\tdetails: payload\n\t};\n\tif (payload.isError === true || result.isError === true) envelope.isError = true;\n\treturn envelope;\n}\nasync function runCodeMode(params) {"
      ],
      [
        "return jsonResult(await runtime.call(call.id, call.input, {",
        "return toolCallResultEnvelope(await runtime.call(call.id, call.input, {"
      ],
      [
        "function applyToolSearchCatalog(params) {\n\tconst config = resolveToolSearchConfig(params.config);\n\treturn applyToolCatalogCompaction({\n\t\t...params,\n\t\tenabled: config.enabled,\n\t\tisVisibleControlTool: (tool) => TOOL_SEARCH_CONTROL_TOOL_NAMES.has(tool.name) && shouldExposeControlTool(tool.name, config.mode)\n\t});\n}",
        "function applyToolSearchCatalog(params) {\n\tconst config = resolveToolSearchConfig(params.config);\n\t// ODS exposes a stable working surface, never a prompt-keyword tool plan.\n\t// Policy has already filtered params.tools; every other permitted tool stays\n\t// in the catalog. Ambiguous names remain deferred, with qualified IDs.\n\tconst odsNativeNames = params.agentId === \"pixel\" && config.mode === \"tools\"\n\t\t? new Set([\"read\", \"write\", \"edit\", \"apply_patch\", \"exec\", \"process\",\n\t\t\t\"web_fetch\", \"web_search\", \"pixel_ods_skill\", \"pixel_ods_ask_user\",\n\t\t\t\"pixel_ods_extensions\", \"pixel_ods_extension_request_status\",\n\t\t\t\"pixel_ods_extension_request_prepare\", \"pixel_ods_extension_request_advance\",\n\t\t\t\"pixel_ods_extension_proposal\", \"pixel_ods_python_library_proposal\"])\n\t\t: new Set();\n\tconst uniqueNames = collectUniqueCatalogToolNames(params.tools);\n\treturn applyToolCatalogCompaction({\n\t\t...params,\n\t\tenabled: config.enabled,\n\t\t...(odsNativeNames.size ? {\n\t\t\tisVisibleCatalogTool: (tool) => odsNativeNames.has(tool.name) && uniqueNames.has(tool.name)\n\t\t} : {}),\n\t\tisVisibleControlTool: (tool) => TOOL_SEARCH_CONTROL_TOOL_NAMES.has(tool.name) && shouldExposeControlTool(tool.name, config.mode)\n\t});\n}"
      ]
    ],
    "d0976a510f4c0a452c27a5cff42f3d26e3f133daa67cfbbe295ac600db6beccc": [
      [
        "async function runCodeMode(params) {",
        "/** Preserve native images instead of JSON-encoding their bytes as prompt text. */\nfunction toolCallResultEnvelope(payload) {\n\tconst result = isRecord(payload?.result) ? payload.result : void 0;\n\tconst content = result?.content;\n\tconst validImage = (block) => isRecord(block) && block.type === \"image\"\n\t\t&& typeof block.data === \"string\" && block.data.length > 0\n\t\t&& typeof block.mimeType === \"string\" && /^image\\/[a-z0-9.+-]+$/i.test(block.mimeType);\n\tconst validText = (block) => isRecord(block) && block.type === \"text\" && typeof block.text === \"string\";\n\t// Leave unfamiliar result shapes to the existing serializer. Do not silently\n\t// drop unsupported blocks or claim a malformed image is a native attachment.\n\tif (!Array.isArray(content)\n\t\t|| !content.every((block) => validText(block) || validImage(block))) return jsonResult(payload);\n\tif (!content.some(validImage)) {\n\t\t// jsonResult tools may include their entire details object twice: once as\n\t\t// JSON text and once as structured metadata. Project only a byte-identical\n\t\t// JSON rendering out of the model-visible envelope; retain the original\n\t\t// payload out of band for framework consumers and provenance.\n\t\tif (content.length === 1 && validText(content[0]) && isRecord(result.details)\n\t\t\t&& content[0].text === JSON.stringify(result.details, null, 2)) {\n\t\t\tconst projected = { ...result };\n\t\t\tdelete projected.details;\n\t\t\treturn { ...jsonResult({ ...payload, result: projected }), details: payload };\n\t\t}\n\t\t// Exec may repeat the same bytes in content and details.aggregated.\n\t\t// Remove only an exact duplicate from model-visible JSON; distinct output,\n\t\t// status, opaque process handles and the original provenance remain intact.\n\t\tif (content.length === 1 && validText(content[0]) && isRecord(result.details)\n\t\t\t&& result.details.aggregated === content[0].text) {\n\t\t\tconst details = { ...result.details };\n\t\t\tdelete details.aggregated;\n\t\t\treturn { ...jsonResult({ ...payload, result: { ...result, details } }), details: payload };\n\t\t}\n\t\treturn jsonResult(payload);\n\t}\n\tconst tool = isRecord(payload.tool) ? payload.tool : {};\n\tconst identity = Object.fromEntries([\"id\", \"source\", \"sourceName\", \"name\"]\n\t\t.filter((key) => typeof tool[key] === \"string\").map((key) => [key, tool[key]]));\n\tconst envelope = {\n\t\t...result,\n\t\tcontent: [\n\t\t\t...(Object.keys(identity).length ? [{ type: \"text\", text: JSON.stringify({ tool: identity }) }] : []),\n\t\t\t...content\n\t\t],\n\t\t// Framework-owned provenance is retained for ODS receipts and adapters.\n\t\tdetails: payload\n\t};\n\tif (payload.isError === true || result.isError === true) envelope.isError = true;\n\treturn envelope;\n}\nasync function runCodeMode(params) {"
      ],
      [
        "return jsonResult(await runtime.call(call.id, call.input, {",
        "return toolCallResultEnvelope(await runtime.call(call.id, call.input, {"
      ],
      [
        "function applyToolSearchCatalog(params) {\n\tconst config = resolveToolSearchConfig(params.config);\n\treturn applyToolCatalogCompaction({\n\t\t...params,\n\t\tenabled: config.enabled,\n\t\tisVisibleControlTool: (tool) => TOOL_SEARCH_CONTROL_TOOL_NAMES.has(tool.name) && shouldExposeControlTool(tool.name, config.mode)\n\t});\n}",
        "function applyToolSearchCatalog(params) {\n\tconst config = resolveToolSearchConfig(params.config);\n\t// ODS exposes a stable working surface, never a prompt-keyword tool plan.\n\t// Policy has already filtered params.tools; every other permitted tool stays\n\t// in the catalog. Ambiguous names remain deferred, with qualified IDs.\n\tconst odsNativeNames = params.agentId === \"pixel\" && config.mode === \"tools\"\n\t\t? new Set([\"read\", \"write\", \"edit\", \"apply_patch\", \"exec\", \"process\",\n\t\t\t\"web_fetch\", \"web_search\", \"pixel_ods_skill\", \"pixel_ods_ask_user\",\n\t\t\t\"pixel_ods_extensions\", \"pixel_ods_extension_request_status\",\n\t\t\t\"pixel_ods_extension_request_prepare\", \"pixel_ods_extension_request_advance\",\n\t\t\t\"pixel_ods_source_proposal\", \"pixel_ods_python_library_proposal\"])\n\t\t: new Set();\n\tconst uniqueNames = collectUniqueCatalogToolNames(params.tools);\n\treturn applyToolCatalogCompaction({\n\t\t...params,\n\t\tenabled: config.enabled,\n\t\t...(odsNativeNames.size ? {\n\t\t\tisVisibleCatalogTool: (tool) => odsNativeNames.has(tool.name) && uniqueNames.has(tool.name)\n\t\t} : {}),\n\t\tisVisibleControlTool: (tool) => TOOL_SEARCH_CONTROL_TOOL_NAMES.has(tool.name) && shouldExposeControlTool(tool.name, config.mode)\n\t});\n}"
      ]
    ]
  }
}
